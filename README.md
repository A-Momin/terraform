-   `$ git init`
-   `$ git config --local user.email "bbcredcap3@gmail.com"`
-   `$ git config --local user.name "Aminul Momin"`
-   `$ git remote add origin git@gh1:A-Momin/terraform.git`
-   `$ ssh -T git@gh1`
-   `$ git branch -M master`
-   `$ git push origin master`

---

```sh
git init && git config --local user.email "bbcredcap3@gmail.com" && git config --local user.name "Aminul Momin" && git remote add origin git@gh1:A-Momin/terraform.git && ssh -T git@gh1
```
